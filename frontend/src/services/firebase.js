/**
 * Firebase Configuration
 * NOTE: Firebase is currently deprioritized.
 * The application uses backend authentication instead.
 * This file is kept for future optional Firebase integration.
 */

// Firebase configuration (optional - currently not used)
export const firebaseConfig = {
  // Credentials removed - using backend authentication
  // Add credentials here if Firebase is needed in the future
};

// Placeholder exports for backward compatibility
export const auth = null;
export const googleProvider = null;

// Placeholder auth object
export const firebaseAuth = null;

export default app;
